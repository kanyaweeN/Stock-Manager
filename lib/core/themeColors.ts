/**
 * สีสองค่านี้เป็น **ข้อยกเว้นเดียว** ของกฎ "ห้าม hardcode สีนอก CSS"
 *
 * `viewport.themeColor` (แถบสถานะของเบราว์เซอร์/มือถือ) กับ `manifest.ts` (ตอนเพิ่มไปหน้าจอโฮม)
 * ถูกอ่านโดยระบบปฏิบัติการก่อนที่ CSS จะถูกโหลด — ใส่ `var(--bg)` ไม่ได้ ต้องเป็นค่าสีจริง
 * จึงรวมไว้ที่ไฟล์เดียวแทนที่จะพิมพ์ซ้ำสองที่แล้วเพี้ยนกันเอง (ซึ่งเคยเพี้ยนมาแล้วจริง ๆ:
 * layout ใช้ `#f5f6f8` แต่ `--bg` ใน `app/styles/base.css` เป็น `#f4f5f7`)
 *
 * **แก้ตรงนี้ต้องแก้ `--bg` ใน `app/styles/base.css` ให้ตรงกันด้วยเสมอ**
 */
export const THEME_COLOR_LIGHT = "#f4f5f7";
export const THEME_COLOR_DARK = "#0f1216";
