# stock-manager

Next.js (App Router) + TypeScript stock/inventory tracker. Thai UI. No backend — data persists client-side (OPFS/localStorage) with optional Google Drive sync.

## ตรวจก่อนบอกว่าเสร็จ

```bash
npm test        # vitest — เทสต์ชั้นคำนวณล้วนๆ ใน lib/__tests__/ (203 เคส)
npm run typecheck
npm run lint
```

เทสต์อยู่ที่ `lib/__tests__/*.test.ts` และ **import โมดูลจริง** ไม่ใช่ก๊อปตรรกะมาเขียนใหม่
(ถ้าก๊อป เทสต์จะเขียวต่อไปแม้โค้ดจริงพัง) ครอบเฉพาะ `lib/` ที่เป็นฟังก์ชันบริสุทธิ์ —
ตั้งใจไม่ลง jsdom เพิ่ม ของที่ต้องใช้ DOM (`lib/shopee.ts` ที่ใช้ `DOMParser`, ตัว component)
พิสูจน์ด้วยการกดจริงในเบราว์เซอร์คุ้มกว่า

**สิ่งที่ต้องมีเทสต์เสมอเมื่อแตะ:**
- `lib/db.ts` — migration ทุก step ต้อง**รันซ้ำได้โดยผลไม่เปลี่ยน** (ข้อมูลไม่มี `schemaVersion` จะ replay ทุก step) `lib/__tests__/db.test.ts` วนเช็คให้ทุก step อัตโนมัติ เพิ่ม step ใหม่ก็ถูกครอบเองทันที
- `overwriteRisks` ใน `lib/useGoogleDriveSync.ts` — ด่านสุดท้ายก่อนเขียนทับไฟล์เดียวที่มีบน Drive ทดสอบผ่าน UI ไม่ได้ (ต้องล็อกอิน Google จริง) จึง `export` ไว้ให้เทสต์เรียกโดยเฉพาะ
- ยอดเงินทุกตัวใน `lib/summary.ts` / `lib/plan.ts` / `lib/cost.ts`

## Data flow

`app/page.tsx` (Home) is the main list view; the other routes (`/config`, `/analyze`, `/cost`, `/summary`, `/plan`) read the same DB. Home composes:

- `lib/StockDBProvider.tsx` — React context wrapping `usePersistedStockDB()`. Call `useStockDB()` to get `{ db, setDb, status }`. `db: StockDB = { schemaVersion, items, categoryPresets, avoidIngredients?, skinProfile?, recipes?, plans?, pricing?, updatedAt? }`.
- `lib/usePersistedDB.ts` — persistence engine. Priority: OPFS file → localStorage fallback → optional user-picked file handle. Don't touch unless changing storage strategy.
- `lib/useProductFilters.ts` — search/category/stock-tab/sort state + derived `filtered` list. All list-view filtering logic lives here. Default sort is `"bought-desc"` (ซื้อล่าสุด) = `item.purchasedAt` desc — **not** `"added-desc"` (เพิ่มล่าสุด, `item.createdAt`), because a repeat Shopee purchase merges into the existing row and leaves `createdAt` untouched, so just-bought items never surface under `added-desc`. Both fall back to position in `db.items` when dates tie or are missing.
- `lib/useProductActions.ts` — CRUD: `save`, `remove`, `inc`, `dec`, `groupItems`, `ungroup`, `setCatsForItems`, `importFromShopee`, `exportCsv`.
- `lib/stock.ts` — **ชั้นล่างสุด ไม่ import อะไรนอกจาก types** คำถามพื้นฐานเรื่องของ 1 ชิ้น: `isLow`, `isOutOfStock`, `remainingUnits`, `countUnits` — เกณฑ์พวกนี้เคยกระจายอยู่ใน `useProductFilters` (ไฟล์ฮุก), `ProductGrid` และ `plan.ts` แบบเขียนซ้ำกันเอง **ห้ามเขียน `i.qty <= i.min` เองอีก ให้เรียกจากที่นี่**
- `lib/db.ts` — `StockDB` type, `DEFAULT_DB`, and versioned migrations. `db.schemaVersion` tracks the shape; `MIGRATIONS` is an ordered list of `{ to, note, up(rawDb) }` steps and `CURRENT_SCHEMA_VERSION` is derived from the last one. `migrateDB(raw)` runs only the steps newer than the data's version, then always runs `normalizeDB` (defaults + type coercion + parent-category dedupe). **Changing the StockItem/StockDB shape ⇒ append a new step to `MIGRATIONS`** (data without `schemaVersion` counts as v0 and replays every step, so each step must be idempotent). Pure default-filling needs no step — put it in `normalizeDB`.

## Data model (`lib/types.ts`)

```ts
StockItem { id, name, cats: string[], qty, min, note, img?, link?, status?, source?: "shopee"|"", price?, size?, unit?, packAmount?, location?, openPct?, reorderQty?, variant?, shop?, expiryAt?, openedAt?, paoMonths?, usageLog?, deletedAt?, groupId?, groupName?, purchasedAt?, createdAt?, ingredients? }
```

- `cats` is an **array** (multi-category since 2026-07-20). Anywhere `cat` (singular) appears, it's legacy — check `db.ts` migration.
- `createdAt` (ISO) is set **once** when the item is created and never touched by edits or repeat purchases — that's what "เพิ่มล่าสุด" sorts on. `purchasedAt` (YYYY-MM-DD) is the opposite: it *does* bump on a repeat Shopee import. `/summary` doesn't sum on it though — it sums `priceHistory` points (see the summary row below); `purchasedAt` only fills in the date of a point that has none.
- `shop` (ร้านที่ซื้อ**ครั้งล่าสุด**) เดินหน้าพร้อม `purchasedAt` — ประวัติร้านของทุกครั้งอยู่ที่ `priceHistory[].shop` ต่างหาก การนำเข้าออเดอร์เก่าย้อนหลังจึงลงได้แค่ในประวัติ ไม่ทับร้านล่าสุด
- `expiryAt`/`openedAt`+`paoMonths` เป็น **สองทาง**ของวันหมดอายุที่มักไม่ตรงกัน อย่าอ่านฟิลด์ดิบตรงๆ ให้เรียก `effectiveExpiry()` เสมอ (ดูตารางข้างล่าง)
- `unit`/`packAmount` คือขนาดบรรจุแบบ**ตัวเลขจริง** (`size` เป็นข้อความอิสระที่ `/cost` ต้องเดาเอา) กรอกแล้ว `lineFromItem` จะใช้ตัวนี้ก่อน `parsePackSize(size)` เสมอ
- `usageLog` จดให้อัตโนมัติตอนกด +/− บนการ์ดเท่านั้น (ดูตารางข้างล่าง) — การแก้ `qty` ในฟอร์มไม่จด เพราะนั่นคือ "แก้ตัวเลขให้ถูก" ไม่ใช่ "ใช้ไป"
- `qty` นับ**แพ็คเต็มๆ** เท่านั้น ขวดที่ใช้ไปครึ่งยังนับเป็น 1 — เศษอยู่ที่ `openPct` แยกต่างหาก **อย่าอ่าน `qty` ตรงๆ เวลาต้องการ "เหลือเท่าไรจริงๆ" ให้เรียก `remainingUnits()`**
- `deletedAt` มีค่าเฉพาะของที่อยู่ใน `StockDB.trash` — **ของที่ลบถูกย้ายออกจาก `db.items` จริง** ไม่ใช่ซ่อนด้วยแฟล็ก โค้ดที่วน `db.items` จึงไม่ต้องกรองอะไรเพิ่ม (ดูตารางข้างล่าง)
- `ImportCandidate` (Shopee import staging row) carries `cats: string[]` + `mergeExisting`/`mergeFields` for the repeat-purchase flow — a candidate with `existingId` merges into the existing item instead of adding a row.

## Where things live

| Concern | File |
|---|---|
| **App shell** — sidebar (≥861px) / bottom tab bar (mobile), sync status, version | `components/AppShell.tsx`, mounted once in `app/layout.tsx`. Every page gets nav for free, so pages must NOT add their own "← กลับหน้าหลัก" link. Add a route ⇒ add it to `NAV` |
| Product card grid | `components/ProductGrid.tsx` — same markup for both breakpoints; CSS turns the card into a horizontal list row under 640px. Per-card ⋯ menu (แก้ไข/ใส่สูตร/ลบ) lives here, so `.product-card` must stay `overflow: visible` |
| **เปลือกของกล่องโมดัลทุกอัน** | `components/ModalShell.tsx` — โครง `.modal-backdrop > .modal > .modal-header` เดิมถูกก๊อปไว้ 5 ไฟล์พร้อม `useEffect` ดัก Escape ที่เหมือนกันเป๊ะ **กล่องใหม่ให้ใช้ตัวนี้เสมอ** แล้วส่งแค่ `.modal-body` กับ `.modal-actions` เป็น children · จัดการให้ครบ: `role="dialog"`/`aria-modal`/`aria-labelledby`, กัก Tab ไว้ในกล่อง, คืนโฟกัสให้ตัวที่กดเปิดตอนปิด, Escape ปิด · `closeOnBackdrop` เปิดเฉพาะกล่องที่ "ดูเฉยๆ" ไม่ใช่กล่องที่กรอกฟอร์มค้างอยู่ · `autoFocus` ในกล่องยังทำงานปกติ shell ไม่แย่งโฟกัส |
| ตรรกะจับคู่/รวมรายการตอนนำเข้า Shopee | `lib/importMerge.ts` — `findExisting` (จับคู่ผิด = จำนวนไปบวกใส่ของผิดชิ้น), `mergeWithinBatch` (Shopee เรนเดอร์แถวเดียวกันซ้ำ mobile/desktop), `applyPriceMode` (หารผิด = ต้นทุนกับยอดสรุปเพี้ยนหมด), `linkToExisting` · แยกออกจาก `ImportModal` เพราะตอนอยู่ในไฟล์ component เทสต์ไม่ได้เลย ทั้งที่พลาดแล้วยอดเงินเพี้ยนเงียบๆ |
| Add/edit product modal | `components/ProductModal.tsx` (เฉพาะ JSX + handler) |
| **ตัวแปลง "ของจริง ↔ ค่าที่กรอกในฟอร์ม"** | `lib/productForm.ts` · `lib/recipeDraft.ts` · `lib/planDraft.ts` — ทั้งสามไฟล์รูปแบบเดียวกัน: ชนิด draft + `toXxx`/`fromXxx` **คู่เดียว** ที่เป็นจุดเดียวที่รู้ว่าฟิลด์ไหนแปลงยังไง **เพิ่มฟิลด์ใหม่ = แก้ชนิด + สองฟังก์ชันนี้เท่านั้น** (เดิมกระจายอยู่ในไฟล์ component ตามจุดต่างๆ ลืมที่ใดที่หนึ่งแล้วค่าหายเงียบๆ) · ตัวเลขทุกช่องเก็บเป็น**สตริง** จะได้ลบให้ว่างได้ระหว่างพิมพ์ · **เส้นแบ่งที่พลาดบ่อย: `0` (ตั้งใจกรอก เช่นราคาของแถม) ต่างจาก ว่าง (ยังไม่ได้ตั้ง)** — `productForm` ใช้ `numOrUndef` กับช่องแรก และ `positiveOrUndef`/`positiveIntOrUndef` กับช่องที่ 0 ไม่มีความหมาย ส่วน `recipeDraft.packAmount` **ห้าม fallback เป็น 1** เพราะจะทำให้ต้นทุนพุ่งเป็นราคาเต็ม × ปริมาณที่ใช้ · ครอบด้วย `lib/__tests__/drafts.test.ts` |
| **กล่อง "เลือกจากสต็อก"** | `components/StockPicker.tsx` (เปลือก + แถว) + `lib/stockSearch.ts` (ตัวค้น) — โครง `.stock-picker` เดิมถูกก๊อปไว้ 4 ชุด (สูตรต้นทุน / แผนซื้อของ / รายการที่แนะนำในแผน) พร้อมท่ารูปย่อ + `eslint-disable` ของ `<img>` ที่เหมือนกันเป๊ะ และ `searchStockItems` ก็ถูกเขียนซ้ำใน `RecipeModal`/`PlanModal` ทั้งที่อยู่ในไฟล์ component จึงเทสต์ไม่ได้ · `StockPicker` **ถือคำค้นไว้เอง** ผู้เรียกจึงไม่ต้องมี state คำค้นกับตัวรีเซ็ตมันอีก แถวต่างกันแค่ `meta` (ข้อความด้านขวา) · เพิ่มฟิลด์ที่ค้นได้ = แก้ `searchStockItems` ที่เดียว ครอบด้วย `lib/__tests__/stockSearch.test.ts` |
| "ใส่ของที่เลือกไว้ในสูตร/แผนไหน?" | `components/AddToTargetModal.tsx` — กล่องเดียวใช้ได้ทั้งสองทาง (เดิมเป็นสองก้อนใน `app/page.tsx` ที่ต่างกันแค่คำกับข้อความสรุปของแต่ละแถว) |
| Category multi-select (used in filter toolbar AND product modal) | `components/CategoryMultiSelect.tsx` — pass `allowCreate` to let user type a brand-new category inline |
| Shopee HTML paste-and-parse import | `components/ImportModal.tsx` + `lib/shopee.ts` (regex/DOM scraping of pasted page source) |
| Home toolbar — search, filter chips row, primary button, ⋯ menu | `components/Toolbar.tsx`. **นำเข้าจาก Shopee คือทางหลักที่ของเข้าสต็อก** จึงเป็นปุ่ม primary บนแถบ (และ FAB ตัวใหญ่บนมือถือ) ส่วน "เพิ่มเอง" เป็นปุ่มรอง/FAB ตัวเล็กที่ซ้อนอยู่ด้านบน — คำสั่งที่ใช้นานๆ ครั้ง (ส่งออก/สร้างสูตร/เลือกหลายอัน) อยู่ในเมนู ⋯ |
| Clickable count chips (ยุบ StatsBar + stock-tabs เดิมเป็นแถวเดียว) | `components/FilterChips.tsx` — one chip active at a time; `ChipKey = StockTab \| "uncategorized"`. `.stats`/`.stat` CSS still exists for `/cost` + `/summary` |
| ถังขยะ (ลบแล้วกู้คืนได้) | `lib/trash.ts` + `useProductActions.remove`/`removeMany`/`restoreFromTrash`/`deleteForever`/`emptyTrash` + ส่วนท้ายแท็บ "สำรอง/กู้คืน" ใน `components/config/BackupTab.tsx` · **ออกแบบเป็นย้ายของออกจาก `db.items` ไป `db.trash` ไม่ใช่ติดแฟล็กซ่อน** เพราะทั้งแอปวน `db.items` ตรงๆ หลายสิบที่ (กรอง/สรุปยอด/สูตร/แผน/ส่งออก) ถ้าใช้แฟล็กแล้วลืมกรองที่ไหนที่หนึ่ง ของที่ "ลบแล้ว" จะโผล่กลับมาในยอดเงียบๆ · เก็บได้ `TRASH_MAX` (100) เกินแล้วตัวเก่าหลุดถาวร · กู้คืนตอน id ชนกับของในสต็อกจะออก id ใหม่ให้ |
| ของจะหมดอีกกี่วัน (ทำนายจากอัตราการใช้) | `lib/usage.ts` — `usageLog` (วันละจุด, `delta` ติดลบ = ใช้ไป) ถูกจดที่เดียวคือ `useProductActions.changeQty` ที่ `inc`/`dec` เรียก · `usageStats` **ไม่นับจุดแรก** เพราะจุดแรกคือจุดเริ่มจับเวลา ไม่ใช่การใช้ที่เกิดภายในช่วงที่วัด (นับด้วยจะได้อัตราสูงเกินจริง) และ **คืน `null` เมื่อข้อมูลไม่พอ = ไม่รู้ ไม่ใช่ "ยังอีกนาน"** เกณฑ์อยู่ที่ `USAGE_MIN_POINTS`/`USAGE_MIN_DAYS`/`RUNOUT_SOON_DAYS` · `pushUsage` รวมการกดหลายครั้งในวันเดียวเป็นจุดเดียวและ cap ที่ `USAGE_LOG_MAX` เพราะไฟล์นี้ซิงก์ขึ้น Drive ทั้งก้อน ใช้ 3 ที่: ป้าย `📉 หมดใน ~n วัน` บนการ์ด, `suggestForPlan` (`reason: "running-out"` แทรกก่อน `rebuy`) และแผงสรุปใน `ProductModal` — **คนละเรื่องกับ `isLow`** ที่ดูแค่ `qty` เทียบ `min` |
| วันหมดอายุ (ของหมดอายุ/ใกล้หมดอายุ) | `lib/expiry.ts` — วันหมดอายุมี 2 ทาง: **ฉลาก** (`expiryAt`) กับ **PAO** (เปิดแล้วใช้ได้กี่เดือน = `openedAt` + `paoMonths` ตามสัญลักษณ์กระปุกเปิดฝาข้างขวด) `effectiveExpiry()` คืน**ตัวที่มาถึงก่อน**พร้อมบอกว่ามาจากทางไหน — ของที่ไม่ได้กรอกอะไรเลยคืน `null` = **ไม่รู้ ไม่ใช่ "ยังไม่หมดอายุ"** ห้ามเอาไปนับรวมกับของที่ปลอดภัย เกณฑ์ "ใกล้หมดอายุ" อยู่ที่ `EXPIRY_SOON_DAYS` (60) ที่เดียว ใช้ 4 ที่: ชิป `⏰ ใกล้หมดอายุ` (`StockTab = "expiring"`), ตัวเรียง `expiry-asc`, ป้ายบนการ์ดใน `ProductGrid` และกล่องสรุปใน `ProductModal` — `paoExpiry` หนีบวันที่ล้นเดือนกลับมาเป็นวันสุดท้ายของเดือน (31 ม.ค. + 1 เดือน ต้องได้ 28 ก.พ. ไม่ใช่ 3 มี.ค. ซึ่งเป็นการ**ยืดอายุ**ให้ยาวกว่าจริง) |
| ค่าส่ง/ส่วนลดต่อออเดอร์ + **ชื่อร้าน** | `lib/orders.ts` (ทุกอย่างที่เกี่ยวกับออเดอร์อยู่ไฟล์นี้ไฟล์เดียว: เรียง/หาซ้ำ/ยอดก้อนเดียว/`orderExtras`+`extrasInRange`+`totalExtras` และ `normalizeShopName`/`shopKey`) + `lib/useOrderActions.ts` + แถวแก้ไขท้ายหน้า `/summary` · **`useOrderActions` มีแค่ `patchOrder`/`removeOrder` ไม่มี `add`** เพราะออเดอร์ต้องเกิดพร้อม `orderId` บนจุดราคาในธุรกรรมเดียว การเพิ่มลอยๆ ทีหลังจะได้ก้อนเงินที่ไม่มีสินค้าชิ้นไหนอ้างถึง — และลบออเดอร์**ไม่แตะ** `priceHistory[].orderId` ที่ค้าง (ไม่มีผลกับการคำนวณ เพราะหน้าสรุปวนจาก `db.orders` ไม่ใช่จากจุดราคา) · `findDuplicateOrder` เตือนตอนนำเข้าหน้าเดิมซ้ำ เพราะค่าส่งที่ถูกนับสองรอบทำให้ยอดเกินจริงแบบ**เงียบๆ** ต่างจากสินค้าที่ซ้ำแล้วเห็นจำนวนเด้ง · `lib/summary.ts` เป็นคนเอา `orderExtras` ไปรวมกับ `priceHistory` ของสินค้าอีกที · **ชื่อร้านต้องผ่าน `normalizeShopName`/`shopKey` เสมอ** (ทั้ง `PurchaseOrder.shop`, `StockItem.shop`, `PricePoint.shop`) — `normalizeShopName` = ชื่อที่ใช้**โชว์** (บีบช่องว่างซ้อน), `shopKey` = คีย์ที่ใช้**จัดกลุ่ม** ซึ่งตัดช่องว่างทิ้ง**ทั้งหมด** เพราะภาษาไทยไม่เว้นวรรคระหว่างคำ `"ร้านเอ"` กับ `"ร้าน เอ"` คือร้านเดียวกัน ถ้าจัดกลุ่มด้วยชื่อที่โชว์จะแตกเป็นสองแถวแบ่งยอดกันเอง และ `widestShopGap` จะเอาร้านเดียวกันมาเทียบราคากับตัวเอง · เก็บแยกจาก `PricePoint` เพราะเป็นเงินระดับ**ออเดอร์** (ออเดอร์เดียวมีของหลายชิ้น แปะไว้ที่จุดราคาจะถูกนับซ้ำเท่าจำนวนชิ้น) จุดราคาชี้กลับมาด้วย `orderId` — สร้างที่เดียวคือ `useProductActions.importFromShopee(chosen, extras)` และจะสร้างก็ต่อเมื่อค่าส่งหรือส่วนลดไม่ใช่ 0 ทั้งคู่ · เงินก้อนนี้เข้ายอดรวม/ยอดรายเดือนของ `/summary` แต่ **ไม่เข้าตารางแยกตามหมวดหมู่/ร้าน/รายชิ้น** เพราะหารลงสินค้าแต่ละชิ้นไม่ได้โดยไม่เดา (และไม่เพิ่ม `times`/`qty` เพราะไม่ใช่ "ครั้งที่ซื้อของ") |
| "ซื้อบ่อย" (นับให้อัตโนมัติ ไม่ต้องติ๊กเอง) | `lib/price.ts` — `buyTimes(item)` = จำนวนจุดใน `priceHistory` (ของเก่าที่ยังไม่มีประวัติแต่มี `purchasedAt` นับเป็น 1) และ `isFrequent` = ถึง `FREQUENT_MIN_TIMES` (3) เกณฑ์อยู่ที่ค่าคงที่ตัวนี้ที่เดียว ใช้ 4 ที่: ชิป `🔁 ซื้อบ่อย` (`StockTab = "frequent"`), ตัวเรียง `times-desc`/`times-asc` (นับครั้งล่วงหน้าเป็น Map ใน `sorters` — `buyTimes` วน `priceHistory` ทุกครั้ง เรียกในตัวเปรียบเทียบตรงๆ ไม่ได้), ป้ายบนการ์ดใน `ProductGrid`, และ `suggestForPlan` (`reason: "frequent"` เรียงท้ายสุดเพราะของยังไม่หมด) — **คนละเรื่องกับ `fav` (ปักดาวเอง) และ `status: "rebuy"` (ติดธงเอง)** |
| Reusable labeled input w/ copy·paste·clear buttons | `components/TextField.tsx` |
| ส่งออกไฟล์ (CSV ทุกหน้า + สำรองข้อมูล JSON) | `lib/download.ts` — `csvCell`/`csvRows`/`downloadFile` เดิมท่านี้ถูกก๊อปไว้ 4 ที่ (สินค้า/สูตร/แผน/แบ็กอัป) กติกา escape กับ **BOM** จึงเพี้ยนไม่ตรงกันได้เงียบๆ · `downloadFile` ใส่ BOM ให้เองเมื่อเป็น csv — ขาด BOM แล้ว Excel บนวินโดวส์อ่านภาษาไทยเป็นขยะ |
| โหมดเลือกหลายอันของหน้าแรก | `lib/useSelection.ts` (state) + `components/SelectActionBar.tsx` (แถบคำสั่งท้ายจอ) — สถานะชุดนี้ต้องเปลี่ยนพร้อมกันเสมอ (โหมด + เซ็ต id) ปนอยู่ใน `page.tsx` กับ state ของโมดัลอีก 6 ตัวแล้วลืม `exitSelectMode` ได้ง่าย · "เลือกทั้งหมด" หมายถึงเฉพาะที่ผ่านตัวกรองอยู่ตอนนี้ ไม่ใช่ทั้งสต็อก |
| Config page tabs (backup/categories/sheets/storage) | `components/config/*.tsx`, routed at `app/config/page.tsx` |
| Ingredient (INCI) analysis — offline dictionary, tags, conflict rules | `lib/ingredients.ts` — **no network, no API**. Add ingredients to `INGREDIENT_DB`, tag pairs that clash to `CONFLICT_RULES`. New tag ⇒ also add to `TAG_META` + `TAG_PRIORITY` |
| Ingredient analysis UI (tag chips, warnings, parsed list) | `components/IngredientPanel.tsx` — used in `ProductModal` and `/analyze` |
| Multi-product ingredient comparison + user's avoid-list | `app/analyze/page.tsx`. Avoid-list is `StockDB.avoidIngredients` (not per-item) |
| คำนวณต้นทุน (สูตรผลิต → ต้นทุน/ชิ้น, กำไร) | `lib/cost.ts` (คำนวณล้วนๆ ไม่มี state) + `lib/useRecipeActions.ts` (CRUD) + `app/cost/page.tsx` + `components/RecipeModal.tsx`. เก็บใน `StockDB.recipes`; ต้นทุนบรรทัด = `buyPrice ÷ packAmount × usedAmount`. เลือกสินค้าจากสต็อกแล้วดึงราคา/ขนาด (`parsePackSize` แกะ `item.size` เช่น `"1000 g"`) มาเติมให้ เปิด `RecipeModal` ได้จาก 3 ทาง: หน้า `/cost`, ปุ่ม 🧮 บนการ์ดสินค้า (`ProductGrid.onAddToRecipe`) และปุ่มในแถบเลือกหลายอันของ `app/page.tsx` — **ไม่ตัดสต็อกอัตโนมัติ** — `Recipe.runs` (จดว่าทำไปกี่รอบเมื่อไร ผ่าน `useRecipeActions.logRun` ซึ่งเขียนลง db ทันทีไม่รอกดบันทึกสูตร) ก็ไม่ตัดเหมือนกัน เพราะวัตถุดิบวัดเป็นกรัม/มล. แต่ `item.qty` นับเป็นแพ็ค หักอัตโนมัติจะได้จำนวนแพ็คเป็นเศษทศนิยมที่เพี้ยนกับทุกอย่างที่นับเป็นชิ้น · `productionSummary` คิดต้นทุนจากราคาวัตถุดิบ**ปัจจุบัน** ไม่ใช่ราคา ณ วันที่ทำ (สูตรไม่ได้ถ่ายรูปราคาไว้) · สูตรจะติดไปกับ Drive sync + แบ็กอัป JSON (แต่ไม่ไป Google Sheets ที่ส่งออกเฉพาะ items) |
| คิดราคาขาย (ควรขายเท่าไรถึงได้กำไรตามเป้า) | `lib/pricing.ts` (คำนวณล้วนๆ) + `lib/usePricingSettings.ts` + `components/PriceAdvisor.tsx` ที่ฝังอยู่ท้าย `RecipeModal` ค่าตั้ง (เป้ากำไร/ค่าธรรมเนียม/วิธีปัดราคา) เก็บที่ `StockDB.pricing` **ใช้ร่วมกันทุกสูตร** แก้แล้วเซฟทันทีไม่ต้องรอกดบันทึกสูตร — สูตรคือ `ราคา = (ต้นทุน + ค่าส่งต่อชิ้น) ÷ (1 − ค่าธรรมเนียม% − กำไรที่อยากได้%)` ซึ่ง **หารด้วยศูนย์/ติดลบได้** ถ้าค่าธรรมเนียม + เป้ากำไร ≥ 100% กรณีนั้น `priceForMargin` คืน `null` (ห้ามเดาเลขให้) "กำไร %" ทุกที่ในโค้ดนี้คือ % **ของราคาขาย** (margin) ไม่ใช่ % ของต้นทุน (markup) และ `roundPrice` **ปัดขึ้นเสมอ** กำไรจะได้ไม่หลุดเป้า · `recipeTotals(recipe, pricing)` รับ `pricing` เพื่อหักค่าธรรมเนียมออกจากกำไร — เรียกโดยไม่ส่ง = กำไรดิบเหมือนเดิม (`/cost` กับ `RecipeModal` ส่ง, CSV ไม่ส่ง) ประโยคไทยที่อธิบายตัวเลขอยู่ที่ `pricingNotes` ที่เดียว |
| วางแผนซื้อของ (เดือนหน้า/ปีใหม่ต้องซื้ออะไร ซื้อไปแล้วเท่าไร) | `lib/plan.ts` (คำนวณล้วนๆ) + `lib/usePlanActions.ts` (CRUD) + `app/plan/page.tsx` + `components/PlanModal.tsx` เก็บใน `StockDB.plans`. บรรทัดมี `priority` (`must`/`normal`/`maybe` — ไม่กรอก = `normal` อ่านผ่าน `priorityOf` เสมอ) ให้ `planTotals` แยก `mustRemaining`/`maybeRemaining` ตอบว่าขั้นต่ำต้องมีเงินเท่าไรและตัดอะไรออกได้ · `planLineFromItem` ใช้ `item.reorderQty` ก่อนการคำนวณจากขั้นต่ำ · ยอดบรรทัด = `price × qty`; ติ๊ก `bought` แล้วยอดย้ายจาก "ยังต้องจ่าย" ไป "จ่ายไปแล้ว" (ใช้ `paidPrice` ถ้ากรอก) — **ไม่ยุ่งกับสต็อก** สต็อกอัปเดตทางนำเข้า Shopee เท่านั้น เปิด `PlanModal` ได้ 3 ทางเหมือน `RecipeModal`: หน้า `/plan`, เมนู ⋯ บนการ์ดสินค้า (`ProductGrid.onAddToPlan`) และปุ่มในแถบเลือกหลายอันของ `app/page.tsx` — ทางหลังสองทางถ้ายังไม่มีแผนเลยจะข้ามไปสร้างแผนใหม่ให้ทันที และของที่ `itemId` อยู่ในแผนนั้นแล้วจะไม่ถูกเพิ่มซ้ำ `suggestForPlan` ดึงของหมด/ใกล้หมด/ติดธง `rebuy` มาเสนอ ส่วน `boughtHint` เตือนเฉยๆ ว่าของชิ้นนั้นถูกนำเข้าสต็อกหลังสร้างแผนแล้ว (ไม่ติ๊กให้เอง เพราะ `purchasedAt` อาจเป็นการซื้อรอบอื่น) วันครบกำหนดของพรีเซ็ตคิดจาก `endOfMonthISO`/`monthISO` ใน `lib/date.ts` |
| สรุปยอด (จ่ายไปเท่าไร เดือนไหน หมวดไหน ร้านไหน) | `lib/summary.ts` (คำนวณล้วนๆ) + `app/summary/page.tsx`. **ทุกยอดคิดจาก "ครั้งที่ซื้อ" = จุดใน `item.priceHistory` (`spendEvents`) ไม่ใช่ `price × qty` ของสต็อกที่เหลือ** ของที่ใช้หมดแล้วจึงยังนับเป็นยอดของเดือนที่ซื้อ และของที่ซื้อซ้ำจะกระจายยอดไปตามเดือนจริง ไม่กองที่ `purchasedAt` ครั้งล่าสุด — `stockValue` (มูลค่าของที่เหลือ) เป็นยอดคนละตัว จุดที่ไม่มีวันที่ตกไปอยู่แถว "ไม่ทราบวันที่" และไม่เข้าตัวกรองช่วงวัน `byShop` ตอบว่าเงินไหลไปร้านไหน (จาก `priceHistory[].shop` ของเก่าที่ไม่มีร้านกองที่ `UNKNOWN_SHOP`) และ `widestShopGap` หาของที่ราคาต่างกันมากที่สุดระหว่างร้าน โดยเทียบ**ราคาเฉลี่ยถ่วงน้ำหนักต่อร้าน** ไม่ใช่ราคาครั้งเดียว · `summaryInsights` แปลตัวเลขเป็นประโยคไทย (เทียบเดือนก่อน/ค่าเฉลี่ย, หมวดที่กินงบ, ราคาขึ้น, ร้านที่ถูกกว่า, ค่าส่ง, ข้อมูลที่ยังขาด) — เพิ่มประโยคใหม่ที่นี่ที่เดียว |
| Google OAuth token (GIS, client-side only) | `lib/googleAuth.ts` — `requestAccessToken(clientId, scope, silent)`. Shared by Drive + Sheets; same OAuth client id, different scope |
| **Cloud sync (the real one)** — whole `StockDB` as one JSON file in Drive `appDataFolder` | `lib/googleDrive.ts` + `lib/useGoogleDriveSync.ts` + `components/config/DriveTab.tsx`. Schema-agnostic: new StockDB fields sync automatically, nothing to update here. Pull replaces the local DB wholesale (runs `migrateDB` on the way in). **`useGoogleDriveSync` ถูกเรียกที่ `StockDBProvider` ที่เดียว** (หน้า `/config` อ่านผ่าน `useStockDB().driveSync`) เพราะการซิงก์อัตโนมัติต้องทำงานทุกหน้า และรับ `ready` = โหลดข้อมูลในเครื่องเสร็จแล้ว ห้ามซิงก์ก่อนหน้านั้นเด็ดขาด (จะส่ง `DEFAULT_DB` ว่างไปทับไฟล์จริง) · **push ตรวจ `overwriteRisks` ก่อนเขียนทับเสมอ** — เทียบ "จำนวนตอนเปิดแอป" (ไม่ใช่จำนวนปัจจุบัน ไม่งั้นการลบของเองจะโดนเตือนทุกครั้ง) กับสภาพไฟล์บน Drive ที่จำไว้ใน localStorage `stock_manager_drive_seen` มีความเสี่ยง → กดเอง = `confirm`, ซิงก์อัตโนมัติ = หยุดตัวเอง (`autoPaused`) แล้วขึ้นแถบเตือน `.sync-alert` ใน `AppShell` ทุกหน้า |
| Google Sheets **export only** (no pull) | `lib/googleSheets.ts` — **column order is positional**; ranges (`ITEMS_RANGE`, `PRESETS_CELL`, write range) derive from `HEADER.length`, so adding a StockItem field = append to `HEADER` + `itemsToRows`. Append at the END or old sheets shift out of alignment. Pull was removed on purpose — a sheet only holds `items`, so pulling wiped `recipes`/`skinProfile`/uncolumned fields |

## Conventions specific to this repo

- Every user-facing string is Thai. Keep it that way.
- วันที่แบบ `YYYY-MM-DD` (`purchasedAt`, `priceHistory[].date`, `plans[].dueDate`/`lines[].boughtAt`, ช่วงวันใน `/summary`, ชื่อไฟล์ export) ใช้ `todayISO()`/`daysAgoISO()`/`endOfMonthISO()`/`daysUntil()` จาก `lib/date.ts` เท่านั้น — **ห้าม** `new Date().toISOString().slice(0, 10)` เพราะนั่นคือวันที่ตาม UTC ไทยเร็วกว่า 7 ชม. เลยได้ "เมื่อวาน" ตั้งแต่เที่ยงคืนถึง 7 โมงเช้า (`createdAt`/`updatedAt` ที่เป็น timestamp เต็มยังใช้ `toISOString()` ได้ตามปกติ)
- `lib/ingredients.ts` แคชผลไว้ 3 ชั้น (`defsFor`, `itemTags`, `analyzeIngredients`) เพราะทั้งสต็อกถูก parse ใหม่ทุกครั้งที่ `db.items` เปลี่ยน identity — อาร์เรย์/ออบเจ็กต์ที่คืนมาถูกใช้ร่วมกัน **ห้ามแก้ไข** ให้ copy ก่อนถ้าจะ sort/push
- `cats`/CSV/Sheets serialize multi-value fields joined with `"; "` (see `useProductActions.exportCsv`, `googleSheets.ts`).
- **ห้ามเรียก `confirm()`/`alert()` หรืออะไรที่มีผลข้างเคียงข้างใน updater ของ `setDb`** — updater ต้องบริสุทธิ์ เพราะ React เรียกซ้ำได้ (StrictMode เรียกสองรอบใน dev) แล้วผู้ใช้จะเจอกล่องยืนยัน**เด้งสองครั้ง** ให้ถามก่อนแล้วค่อยเรียก `setDb` (ดู `remove`/`removeMany`/`emptyTrash` ใน `useProductActions` — ตัวหลังรับ `count` มาจากผู้เรียกเพื่อให้ถามนอก updater ได้)
- ชื่อร้านถูกบีบด้วย `normalizeShopName` ที่ `normalizeDB` (ทั้ง `item.shop`, `priceHistory[].shop`, `orders[].shop`) จึงครอบคลุมทุกทางเข้าอัตโนมัติ — นำเข้า Shopee/กรอกเอง/กู้คืนไฟล์/ดึงจาก Drive ไม่ต้องบีบเองซ้ำ
- `openPct` ถูกล้างทิ้งอัตโนมัติเมื่อ `qty` ลงถึง 0 (`changeQty`) — ไม่งั้นพอซื้อรอบใหม่ ของที่ยังไม่แกะจะถูกคิดว่าเหลือแค่ n% ตามค่าเก่าที่ค้าง แล้วมูลค่าสต็อกต่ำกว่าจริง
- Filter/sort/tab state all lives in `useProductFilters` — don't duplicate filter state in `page.tsx`.
- Styling is one global stylesheet, no CSS modules: `app/globals.css`, using CSS custom properties (`--bg`, `--panel`, `--card`, `--accent`/`--accent-soft`, `--ok`, `--low`, `--danger`, `--violet` + `-soft` pairs, `--shadow-1/2`) with a `prefers-color-scheme: dark` block. Match that pattern for new components — don't hardcode colors.
- Page root is `.page` (`.wrap` kept as an alias). Breakpoints: **860px** = shell switches sidebar → bottom tab bar (`--side-w`, `--tabbar-h` drive the offsets of `.select-action-bar` and `.fab`), **640px** = product cards switch to list rows.
- Low/out-of-stock is shown as a thin colored bar on the card's left edge (`.product-card.low-row::before`) — not by tinting the whole card, which fought with the ingredient tag colors.
- **พอร์ตต้องเป็น 3000 เสมอ** — OPFS/localStorage ผูกกับ origin เปิดคนละพอร์ต = เจอฐานข้อมูลเปล่า (ผู้ใช้จะนึกว่าข้อมูลหาย) `package.json` พิน `next dev -p 3000` และ `.claude/launch.json` ตั้ง `autoPort: false` — **ห้ามเปิด autoPort กลับ** ถ้าพอร์ตชน ให้ปิดตัวที่ค้างอยู่หรือใช้เซิร์ฟเวอร์เดิมที่รันอยู่แล้ว (ย้ายเครื่องข้าม origin ทำได้ทางเดียวคือ export/import JSON ที่ `/config`)

## Gotchas when testing in the Browser pane

- Browser-tool `form_input`/`computer.type` can silently fail to update React controlled-input state (sets DOM value without a real `input` event). If a save button seems to no-op, verify via `javascript_tool` using the native value setter + dispatched `input` event before concluding there's a real bug.
- After editing hook return shapes (e.g. renaming a state var), do a hard reload / fresh tab — Fast Refresh sometimes shows a stale "hook order changed" error that clears on full reload.
